abbr "ag"="rg"
abbr "aic"="aicommits"
abbr "aica"="aicommits -a"
abbr "aicap"="aicommits -a && git push"
abbr "ci"="git commit -a -v"
abbr "co"="git checkout"
abbr "di"="git diff"
abbr "ga"="git add"
abbr "gau"="git add -u"
abbr "gr"="git grep"
abbr "l"="git log --graph --color=always --pretty='%Cred%h%Creset -%C(auto)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset'"
abbr "lo"="git log -p"
abbr "mysql"="mysqlsh"
abbr "pu"="git pull"
abbr "st"="git status"
